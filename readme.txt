PDF result downloads run directly in the browser. No Python server or package
installation is required.

Open a student result, then select “Download PDF”. The PDF includes student
details, a subject/marks table, total, CGPA, grade, result status, and the
generation date. An internet connection is required the first time so the
browser can load the PDF library used by the website.
